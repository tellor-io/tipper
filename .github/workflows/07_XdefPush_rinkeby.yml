name: XdefPush_rinkeby
on:
  schedule: 
  - cron: "2 0 * * *"  # https://docs.github.com/en/free-pro-team@latest/actions/reference/events-that-trigger-workflows#scheduled-events


jobs:
  XdefPush_rinkeby:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [14.x]
    
    steps:
    - uses: actions/checkout@v2
    - name: Run tests with Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v1
      with:
        node-version: ${{ matrix.node-version }}
    - run: npm install
    - run: node Xdef/01_DataFeed.js rinkeby

      env:
        ETH_PK: ${{ secrets.ETH_PK }}
        ETH_PUB: ${{ secrets.ETH_PUB }}
        INFURA_TOKEN: ${{ secrets.INFURA_TOKEN }}
        RINKEBY_ETH_PK2: ${{ secrets.RINKEBY_ETH_PK2 }}
        RINKEBY_ETH_PUB2: ${{ secrets.RINKEBY_ETH_PUB2 }}